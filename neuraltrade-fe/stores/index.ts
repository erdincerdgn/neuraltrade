import { create } from 'zustand';
// import { createAlertStore } from './alert/alert.store';
// import { createModalStore } from './modal/modal.store';
// import { createRegisterStore } from './register/register.store';

// type ModalStore = ReturnType<typeof createModalStore>;
// type AlertStore = ReturnType<typeof createAlertStore>;
// type RegisterStore = ReturnType<typeof createRegisterStore>;

// type AppStore = ModalStore & AlertStore & RegisterStore;

// export const useAppStore = create<AppStore>((set) => ({
// //   ...createModalStore(set),
// //   ...createAlertStore(set),
// //   ...createRegisterStore(set),
// }));
