# Supabase (service_role recommended for DDL during migrations)
# Replace placeholders with your actual Supabase values
DATABASE_URL="postgresql://postgres:<SERVICE_ROLE_PASSWORD>@db.<ref>.supabase.co:5432/postgres?schema=public"
DIRECT_URL="postgresql://postgres:<SERVICE_ROLE_PASSWORD>@db.<ref>.supabase.co:5432/postgres?schema=public"

# Local Docker Postgres (shadow DB used by Prisma Migrate for diffing)
# Assumes your local container listens on localhost:5432 with user postgres/password postgres
SHADOW_DATABASE_URL="postgresql://postgres:postgres@localhost:5432/prisma_shadow?schema=public"

# JWT secret for AuthModule (set your own secret in development/production)
JWT_SECRET="super-secret"
